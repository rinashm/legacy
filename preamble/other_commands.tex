% text definition
% Выделение определения Сейчас --- полужирное начертание, но эту
% команду можно изменить на что угодно. Можно поросить её покрасить
% текст или выдать что-то совсем другое
\newcommand*{\tdef}[1]{%
	\textbf{#1}%
}

% Как отображается номер для subsection
\renewcommand*{\thesubsection}{\arabic{subsection}}

\renewcommand*{\leq}{\leqslant}
\renewcommand*{\geq}{\geqslant}

\usepackage[all]{xypic}

\newbox\nextitem

\AtBeginDocument{%
	\savebox{\nextitem}{%
		\scalebox{1.5}{%
				\rotatebox{-90}{%
					% \fbox{%
						\mbox{$\leq$}%
					% }%
				}%
		}%
		\raisebox{-1.8ex}{%
			\rlap{%
				\quad
				\color{mcnSecondary}%
				$\xleftarrow{\hbox to 10em {}}$~%
				\text{<<не превосходит>>}%
			}%
		}%
	}%
}

\newcommand*{\insertsucc}{%
	\ar @{} [d] |{\usebox\nextitem}%
}

% vim:ts=2:sw=2:tw=68
